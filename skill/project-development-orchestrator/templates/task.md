# 任务目标

`TODO`：一句话描述业务效果。

# Risk

- 类型：`TODO`（普通任务 / 关键任务）
- 判断理由：`TODO`（按最坏业务后果；关键任务在编码前确认验证和复审强度）
- 关键契约与升级触发条件：`TODO`

# Scope

- 当前基线（完整 commit SHA 或未提交基线说明）：`TODO`
- 允许改动文件/目录：`TODO`
- 禁止改动文件/目录：`TODO`

# 执行器与授权

- 执行器：`TODO`（默认 Claude Code；用户指定时 Cursor 或 Codex）
- 请求模型：`TODO`
- 编码授权：`TODO`（未获得 / 已获得；方案确认不等于编码授权）
- 权限范围：`TODO`

# 真实业务背景

`TODO`

# 已确认的源码事实

`TODO`：仅写有证据的当前事实，不替执行器预设实现。

# 预期正确行为

- 正常路径：`TODO`
- 关键异常/边界路径：`TODO`

# Verify：自动化测试

- 编码 Agent 必须运行：`TODO`
- Pi 必须独立重跑：`TODO`
- 关键任务额外测试/独立复审：`TODO`

# Verify：本机候选运行验收

- 是否改变运行行为：`TODO`（否时写明免除理由）
- 运行方式：`TODO`（Docker Compose / 原生进程 / 构建产物 / 干净安装环境 / 混合）
- 本地依赖及数据库类型/版本：`TODO`
- 构建或安装命令：`TODO`
- 启动命令：`TODO`
- 业务就绪检查：`TODO`
- 正式入口和核心黑盒业务场景：`TODO`
- 必须检查的最终结果：`TODO`
- 必须检查的日志：`TODO`
- 关键失败/边界/恢复场景：`TODO`
- 停止和清理命令：`TODO`
- 测试数据准备与清理：`TODO`
- 禁止连接的域名、数据库和环境：`TODO`
- 环境缺失时的阻断条件：`TODO`

# 验收标准

- `TODO`：逐条写成可验证结果。
- 编码 Agent 自报、单元测试、进程/容器存活均不能单独构成完成。

# 禁止事项

- 禁止读取或输出凭证，禁止未经授权 SSH 或访问生产环境。
- 禁止未经单独授权 commit、push 或部署。
- 禁止 rsync、scp、docker cp 或其他绕开 Git 的代码交付。
- 遇到范围冲突、前提错误或验证受阻时停止并报告，不静默改方案或降低标准。
