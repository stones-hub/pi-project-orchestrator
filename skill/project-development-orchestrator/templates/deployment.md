# 部署与运行验收

> 本文件只写可执行方法和非敏感配置，不写密码、Token、SSH 私钥、数据库密码或
> 真实 API Key。所有命令、地址和路径必须按本项目真实情况填写。

## 一、本机候选运行验收

### 1. 目标与交付形态

- 正式交付形态：`TODO`
- 本机采用的等价运行方式：`TODO`（Docker 只是可选方式）
- 为什么该方式足以验证真实候选：`TODO`

### 2. 隔离和前置条件

- 本地依赖及版本：`TODO`
- 测试数据库、缓存和合成数据：`TODO`
- 监听地址和端口：`TODO`（默认 `127.0.0.1`）
- 防止误连生产的检查：`TODO`
- 旧进程、旧容器、旧制品的隔离方法：`TODO`

### 3. 构建、启动和就绪

- 构建或安装命令：`TODO`
- 启动应用和依赖：`TODO`
- 业务就绪检查：`TODO`
- 如何证明运行的是当前工作区候选：`TODO`

### 4. 黑盒业务验收

- 正式入口：`TODO`
- 核心成功场景：`TODO`
- 必要的失败/边界/恢复场景：`TODO`
- 响应、页面、文件或数据库结果检查：`TODO`
- 日志检查：`TODO`

### 5. 停止和清理

- 停止命令：`TODO`
- 测试数据、临时目录、容器/volume 清理：`TODO`
- 失败现场需要保留时的规则：`TODO`

## 二、目标机器通过 Git 部署

### 1. 部署身份

- 远程 Git 仓库：`TODO`
- remote 名称：`TODO`
- 部署分支：`TODO`
- 待部署版本：每次部署由 Pi 填写并经用户批准的完整 commit SHA

### 2. 目标机器

- 机器名称/连接别名：`TODO`
- 项目绝对路径：`TODO`
- 服务名称：`TODO`

### 3. 部署前检查

- 检查 remote、当前分支和工作区干净：`TODO`
- 检查批准远程分支的分支头等于批准 SHA：`TODO`
- 检查可安全执行 `git pull --ff-only`：`TODO`

工作区不干净、分支不符、远程分支头不是批准 SHA、无法 `git pull --ff-only` 或
目标 SHA 不存在时停止，不覆盖、不清理、不强推。

### 4. Git 更新、构建和重启

- `git fetch` 和 `git pull --ff-only` 更新命令：`TODO`
- 核对目标机 HEAD 等于批准 SHA：`TODO`
- 依赖安装：`TODO`
- 构建：`TODO`
- 重启：`TODO`

代码只通过 Git 更新；禁止 rsync、scp 复制源码、docker cp、压缩包或手工覆盖。

### 5. 部署后验证

- 业务就绪/健康检查：`TODO`
- 核心业务验证：`TODO`
- 实际运行版本身份确认：`TODO`
- 日志检查：`TODO`

### 6. 失败和回滚

- 各步骤失败时的安全停止点：`TODO`
- 回滚目标选择规则：`TODO`
- 回滚命令：`TODO`
- 回滚后的版本、健康和业务验证：`TODO`

回滚需要新的明确授权；不得自动回滚或把结果未知写成成功。
